SELECT 
  u.id,
  u.email,
  u.primeiro_nome,
  u.id_filial,
  f.nome_filial
FROM login_user u
LEFT JOIN filial f ON f.id = u.id_filial
WHERE u.email = {{ inp_login_01.text}}
  AND u.senha = {{ inp_login_02.text }};