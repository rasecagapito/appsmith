INSERT INTO public.login_user
  (login, primeiro_nome, sobre_nome, email, senha,status, id_filial)
VALUES
  (
    {{ input_login.text }},
    {{ inp_nome.text }},
    {{ inp_sobrenome.text }},
    {{ inp_email.text }},
    {{ inp_senha.text }},
		TRUE,
    {{ Number(sel_filial.selectedOptionValues[0]) }}
  )
RETURNING id;